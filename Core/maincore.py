import sys
import Network.networkmanager
import Core.core

if __name__ == '__maincore__':
    #net = threading.Thread(target=Network.networkmanager, daemon=True)
    Network.networkmanager
    Core.core